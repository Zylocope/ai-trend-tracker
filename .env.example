# Only one key is used, and only for one of four sources.
# Google Trends, Hacker News and OpenRouter need no credentials at all,
# and the site itself needs none - it builds from data/snapshot.json.

# https://newsapi.org/register  (optional: without it the news column stays empty)
NEWS_API_KEY=your_newsapi_key
